<%= form_with(model: @donator, url: money_donators_path(@donator), local: true) do |form| %>
    <% if @donator.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(donator.errors.count, "error") %> prohibited this post from being saved:</h2>
  
        <ul>
          <% donator.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>
  
    <div class="field">
      <%= form.label :contact_number %>
      <%= form.text_field :contact_number %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
   